# Claude Code開発メモ

このプロジェクトはClaude Codeを使用してRISC-V OSの実装を改善しました。

## 開発履歴

### 初期段階
- `run.sh`実行時のコンパイルエラーを修正
- 型定義の重複問題を解決（size_t、NULL等）
- printf関数の宣言統一
- リンカエラーの修正

### コード品質向上
- マジックナンバーを定数に置換
- 関数の詳細なドキュメント追加
- エラーハンドリングの改善
- 不要な空白・タブの削除

### ドキュメント整備
- 英語コメントを日本語に翻訳
- 各主要部分に機能説明を追加
- README.mdでファイル構成を詳細説明

## よく使用するコマンド

### ビルドと実行
```bash
./run.sh
```

### 型チェック・リント
現在のプロジェクトでは特定のリントツールを使用していませんが、
ClangのWarningレベルを上げることで品質を保っています。

### テスト
```bash
tests/run_tests.sh
```
ユニットテスト（tests/unit/、QEMU 上で動くテスト用カーネル）と
E2E テスト（tests/e2e/、実際の OS をシリアル経由で操作）をまとめて実行します。

## プロジェクト構造の理解

このOSは以下の階層構造を持ちます：

1. **ハードウェア層**: RISC-V 64bit CPU（RV64、Sv39）
2. **ブートローダー層**: OpenSBI 
3. **カーネル層**: kernel.c, process.c, memory.c
4. **システムコール層**: exception.c
5. **ユーザー空間層**: user.c

## 開発時の注意点

### メモリ管理
- デマンドページング実装により、実際のアクセス時にページ割り当て
- ページサイズは4KB固定
- 仮想アドレス空間は16MBから開始

### プラットフォーム
- QEMU virt と実機で異なる値は platform/<名前>/ に置き、共通コードで #ifdef しない
- 新しい値を足すときは、全プラットフォームの platform.h / platform.ld に揃えて定義する
- milkv-duo の値は実機で未確認（各ファイルの【要確認】を参照）
- コンパイルフラグは build.sh、run.ps1、tests/run_tests.sh の3か所で揃える

### RISC-V固有の実装
- CSR（Control and Status Register）の適切な使用
- ecall命令によるシステムコール
- 特権レベル（User/Supervisor）の切り替え

### デバッグ
- printf関数は簡易実装（%s, %d, %x, %%をサポート。カーネル側は64ビット値用の %ld, %lx も使える）
- QEMUコンソール出力でデバッグ情報を確認
- PANICマクロによる致命的エラーの検出